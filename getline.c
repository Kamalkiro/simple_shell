#include "main.h"

/**
 * _getline - reads a line from stdin
 * @buffer: string from stdin to be tokenized
 * @buffersize: delimiter of tokenization
 * @stream: pointer to where it would get the line from
 * Return: characters read or -1 if it fails or encounters EOF
*/
int _getline(char **buffer, int buffersize, FILE *stream)
{

}